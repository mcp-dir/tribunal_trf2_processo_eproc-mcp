# Ferramentas

Tribunal TRF2: Processo (eproc) expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_trf2_processo_eproc_consultar`
**Input**: `numero_processo` (opcional), `oab` (opcional), `cpf` (opcional), `cnpj` (opcional), `nome_parte` (opcional)

Tribunal TRF2: Processo (eproc), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TRF2: Processo (eproc)
```
